# HO AI LAB

Cloudflare Worker foundation for the HO AI LAB control plane.

Endpoints:
- `/` — service status
- `/health` — JSON health check

Next layers will add the AI Lab control plane, task queue, database,
free-quota router, agents, storage, memory, and provider failover.
